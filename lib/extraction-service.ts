export type EvidenceDraft={category:"MEDICATION"|"INVESTIGATION"|"DIAGNOSIS"|"PROCEDURE"|"ALLERGY"|"MEDICAL_HISTORY"|"CHRONOLOGY";originalWording:string;normalizedValue:Record<string,string>;pageNumber:number;confidence:number;uncertaintyNotes?:string};
const medication=/\b([A-Z][A-Za-z0-9-]{2,})\s+(\d+(?:\.\d+)?\s?(?:mg|mcg|g|ml))\b/g;
const lab=/\b(Hb|Hemoglobin|Glucose|Sugar|Creatinine|TSH|Sodium|Potassium|WBC|Platelets?)\s*[:=-]?\s*(\d+(?:\.\d+)?)\s*([A-Za-z/%^0-9.-]+)?/gi;
export function extractEvidence(pages:Array<{pageNumber:number;extractedText:string}>):EvidenceDraft[]{const out:EvidenceDraft[]=[];for(const p of pages){for(const m of p.extractedText.matchAll(medication))out.push({category:"MEDICATION",originalWording:m[0],normalizedValue:{name:m[1],dose:m[2]},pageNumber:p.pageNumber,confidence:.78,uncertaintyNotes:"Deterministic text pattern; physician verification required."});for(const m of p.extractedText.matchAll(lab))out.push({category:"INVESTIGATION",originalWording:m[0],normalizedValue:{name:m[1],value:m[2],unit:m[3]??""},pageNumber:p.pageNumber,confidence:.72,uncertaintyNotes:"Reference range is not inferred when absent from the document."});}return out;}

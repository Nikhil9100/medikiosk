// Test stub for the `server-only` marker package.
// In the test environment (vitest/jsdom) the module boundary that
// server-only enforces does not apply; the stub keeps imports resolvable.
export {};

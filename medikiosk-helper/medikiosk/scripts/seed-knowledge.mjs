import pg from "pg";const {Client}=pg;if(!process.env.DATABASE_ADMIN_URL){console.error("DATABASE_ADMIN_URL required");process.exit(2)}const docs=[{corpus:"MODERN_MEDICINE",title:"MediKiosk patient-history guidance",source:"MediKiosk curated demo reference",content:"A clinical history should preserve the patient's wording, onset, duration, severity, associated symptoms, relevant medicines, allergies and available prior records. Urgent warning symptoms require clinician assessment; a digital intake tool does not diagnose."},{corpus:"AYURVEDA",title:"Dashavidha Atura Pariksha structure",source:"MediKiosk SIH structured reference",content:"Dashavidha Atura Pariksha in this project records Prakriti, Vikriti, Sara, Samhanana, Pramana, Satmya, Sattva, Ahara Shakti, Vyayama Shakti and Vaya. Observations are recorded by the physician and are never inferred from missing patient data."}];const c=new Client({connectionString:process.env.DATABASE_ADMIN_URL,ssl:{rejectUnauthorized:false}});try{await c.connect();for(const d of docs){const r=await c.query(`INSERT INTO knowledge_documents(corpus,corpus_version,title,source,language,content) VALUES($1,'sih-rc-1',$2,$3,'en',$4) RETURNING id`,[d.corpus,d.title,d.source,d.content]);await c.query(`INSERT INTO knowledge_chunks(document_id,chunk_index,heading,content) VALUES($1,0,$2,$3) ON CONFLICT(document_id,chunk_index) DO NOTHING`,[r.rows[0].id,d.title,d.content]);}console.log("Curated demo knowledge seeded. Replace/extend with appropriately licensed, reviewed sources before clinical production.")}finally{await c.end().catch(()=>{})}

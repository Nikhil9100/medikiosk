import {chromium} from "playwright";const base=process.env.BASE_URL??"http://127.0.0.1:3000";const b=await chromium.launch({headless:true});let failed=false;try{const p=await b.newPage({viewport:{width:390,height:844}});for(const route of ["/patient","/doctor/login","/hospital/login"]){await p.goto(base+route,{waitUntil:"networkidle"});const result=await p.evaluate(()=>{const main=document.querySelector("main");const h1=document.querySelector("h1");const unnamed=[...document.querySelectorAll("button,input,select,textarea")].filter(el=>{const id=el.id;const label=id&&document.querySelector(`label[for=\"${CSS.escape(id)}\"]`);return !label&&!el.getAttribute("aria-label")&&!el.textContent?.trim()}).length;const small=[...document.querySelectorAll("button,a,input,select")].filter(el=>{const r=el.getBoundingClientRect();return r.width>0&&r.height>0&&(r.height<40||r.width<40)}).length;return{main:!!main,h1:!!h1,unnamed,small}});if(!result.main||!result.h1||result.unnamed||result.small){console.error(`FAIL accessibility ${route}`,result);failed=true}else console.log(`PASS accessibility smoke ${route}`)}await p.close()}finally{await b.close()}if(failed)process.exit(1);

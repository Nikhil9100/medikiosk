import { NextResponse } from "next/server";import { z } from "zod";import { databaseConfigured,withStaffTx } from "@/lib/db/pool";import { login,setStaffCookie } from "@/lib/staff-auth";
const Schema=z.object({email:z.string().email().max(254),password:z.string().min(8).max(200)}).strict();
export async function POST(req:Request){if(!databaseConfigured())return NextResponse.json({error:"Staff storage not configured"},{status:503});const parsed=Schema.safeParse(await req.json().catch(()=>null));if(!parsed.success)return NextResponse.json({error:"Invalid credentials"},{status:400});const result=await withStaffTx(undefined,c=>login(c,parsed.data.email,parsed.data.password));if(result.kind==="limited")return NextResponse.json({error:"Too many attempts"},{status:429,headers:{"Retry-After":"300"}});if(result.kind!=="ok")return NextResponse.json({error:"Invalid credentials"},{status:401});const response=NextResponse.json({staff:result.staff});setStaffCookie(response,result.token);return response;}

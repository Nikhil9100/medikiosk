"use client";import { useCallback,useEffect,useState } from "react";import { useParams,useRouter } from "next/navigation";import Link from "next/link";import { DASHAVIDHA } from "@/lib/dashavidha";
type Bundle={case:{caseId:string;sessionId:string;caseStatus:string;language:string;consentStatus:string;createdAt:string;summary:any;identity?:{status:string;abhaLast4:string|null;abhaAddressMasked:string|null}|null};complaints:Array<any>;signals:Array<any>;documents:Array<any>;evidence:Array<any>;interview:Record<string,any>|null;chat:Array<any>;dashavidha:Array<any>;consultation:any;notes:Array<any>};
export default function CasePage(){const {sessionId}=useParams<{sessionId:string}>();const router=useRouter();const [data,setData]=useState<Bundle|null>(null);const [error,setError]=useState("");const [note,setNote]=useState("");const [saving,setSaving]=useState("");const load=useCallback(async()=>{const r=await fetch(`/api/staff/case/${sessionId}`,{cache:"no-store"});if(r.status===401||r.status===403){router.replace("/doctor/login");return;}if(!r.ok){setError("Unable to load this case.");return;}setData(await r.json());setError("")},[sessionId,router]);useEffect(()=>{void load()},[load]);async function action(path:string,body?:unknown){setSaving(path);try{const r=await fetch(`/api/staff/case/${sessionId}/${path}`,{method:"POST",headers:{"content-type":"application/json"},body:body?JSON.stringify(body):undefined});if(!r.ok){setError("The clinical action could not be saved. Your typed data has been kept on screen.");return false;}await load();setError("");return true;}catch{setError("Connection lost. Nothing was confirmed saved; your typed data has been kept on screen.");return false;}finally{setSaving("");}}
async function review(path:string,body:unknown){try{const r=await fetch(`/api/staff/case/${sessionId}/${path}`,{method:"PATCH",headers:{"content-type":"application/json"},body:JSON.stringify(body)});if(!r.ok){setError("The review action could not be saved. Please retry.");return false;}await load();setError("");return true;}catch{setError("Connection lost before the review action was confirmed. Please retry.");return false;}}if(!data)return <div className="console-shell"><main className="console-main">{error||"Loading case…"}</main></div>;const c=data.case;const assessed=new Map(data.dashavidha.map(x=>[x.observation,x]));return <div className="console-shell"><header className="console-header"><div className="console-brand"><strong>MediKiosk · Clinical Review</strong><span>Case {c.caseId}</span></div><Link className="secondary" href="/doctor">← Queue</Link></header><main className="console-main"><div className="console-title"><div><p className="eyebrow">Physician review</p><h1>{c.caseId}</h1><p className="helper">Language {c.language} · Consent {c.consentStatus} · ABHA {c.identity?.status??"NOT_PROVIDED"}{c.identity?.abhaLast4?` · •••• ${c.identity.abhaLast4}`:""}</p></div><div className="actions" style={{margin:0}}>{!data.consultation&&<button className="primary" disabled={!!saving} onClick={()=>void action("consultation")}>Start consultation</button>}{data.consultation?.status==="IN_PROGRESS"&&<button className="primary" disabled={!!saving} onClick={()=>void action("consultation/complete")}>Complete consultation</button>}<a className="secondary" href={`/api/staff/case/${sessionId}/fhir`} target="_blank" rel="noreferrer">FHIR R4 export</a></div></div>{error&&<div className="system-banner error">{error}</div>}{data.signals.filter(x=>x.status==="UNREVIEWED").length>0&&<section className="urgent-box"><strong>Urgent clinical signals require physician assessment</strong>{data.signals.filter(x=>x.status==="UNREVIEWED").map(x=><div key={x.id} className="case-section"><p>{x.summary} — {x.reason}</p><div className="actions" style={{marginTop:8}}><button className="secondary" onClick={async()=>{await review(`signals/${x.id}`,{status:"REVIEWED"})}}>Reviewed</button><button className="danger" onClick={async()=>{await review(`signals/${x.id}`,{status:"ESCALATED"})}}>Escalate</button><button className="secondary" onClick={async()=>{await review(`signals/${x.id}`,{status:"DISMISSED"})}}>Dismiss</button></div></div>)}</section>}<div className="console-grid"><div><section className="panel"><h2>Physician-ready history</h2><div className="case-section"><h3>Complaints</h3>{data.complaints.map((x:any)=><div className="evidence" key={x.id}><strong>{x.position===1?"Chief complaint":"Additional complaint"}</strong><p>{x.complaintText}</p><small>Severity: {x.severity??"not recorded"} · Body area: {x.bodyRegion??"not recorded"}</small></div>)}</div><div className="case-section"><h3>Adaptive interview</h3>{Object.values(data.interview??{}).length?Object.values(data.interview??{}).map((x:any)=><p key={x.questionId}><b>{x.questionId.replaceAll("_"," ")}:</b> {x.state}{x.value?` — ${x.value}`:""}</p>):<p className="helper">No structured interview answers available.</p>}</div><div className="case-section"><h3>Clinical summary draft</h3><pre style={{whiteSpace:"pre-wrap",fontFamily:"inherit"}}>{c.summary?JSON.stringify(c.summary,null,2):"Not generated yet."}</pre></div></section><section className="panel"><h2>Documents & extracted evidence</h2>{data.documents.length===0?<p className="helper">No documents uploaded.</p>:data.documents.map((d:any)=><div className="case-section" key={d.id}><strong>{d.originalFilename}</strong><p className="helper">OCR {d.ocrStatus} · Extraction {d.extractionStatus} · Review {d.verificationStatus}</p>{data.evidence.filter((e:any)=>e.documentId===d.id).map((e:any)=><div className="evidence" key={e.id}><b>{e.category}</b>: {e.originalWording??JSON.stringify(e.normalizedValue)}<div className="helper">Page {e.pageNumber??"?"} · {e.extractionMethod} · {e.verificationState} · provenance {e.provenance}</div>{e.uncertaintyNotes&&<div className="helper">Uncertainty: {e.uncertaintyNotes}</div>}<div className="actions" style={{marginTop:8}}><button className="secondary" onClick={async()=>{await review(`evidence/${e.id}`,{verificationState:"VERIFIED"})}}>Verify</button><button className="danger" onClick={async()=>{await review(`evidence/${e.id}`,{verificationState:"REJECTED"})}}>Reject</button>{e.verificationState!=="UNVERIFIED"&&<button className="secondary" onClick={async()=>{await review(`evidence/${e.id}`,{verificationState:"UNVERIFIED"})}}>Reset</button>}</div></div>)}</div>)}</section><section className="panel"><h2>Anaya patient-assistant conversation</h2>{data.chat.length?data.chat.map((m:any)=><div className="evidence" key={m.id}><b>{m.role}</b><p>{m.content}</p></div>):<p className="helper">No assistant conversation for this visit.</p>}</section></div><aside><section className="panel"><h2>Dashavidha Atura Pariksha</h2><p className="helper">{assessed.size}/10 records exist. Missing remains NOT_ASSESSED and is never inferred.</p><div className="dash-grid">{DASHAVIDHA.map(([key,label,help])=>{const row=assessed.get(key) as any;return <div className="dash-item" key={key}><strong>{label}</strong><p className="helper">{help}</p><textarea aria-label={`${label} observation`} defaultValue={row?.value??""} id={`d-${key}`} rows={2}/><button className="secondary" onClick={async()=>{const el=document.getElementById(`d-${key}`) as HTMLTextAreaElement;setSaving(key);const r=await fetch(`/api/staff/case/${sessionId}/dashavidha`,{method:"PUT",headers:{"content-type":"application/json"},body:JSON.stringify({observation:key,state:el.value.trim()?"OBSERVED":"NOT_ASSESSED",value:el.value.trim()||null})});if(r.ok)await load();else setError("Dashavidha observation was not saved.");setSaving("")}}>Save</button></div>})}</div></section><section className="panel"><h2>Doctor notes</h2>{data.notes.map((n:any)=><p key={n.id}>{n.body}</p>)}<div className="field"><label htmlFor="note">Add clinical note</label><textarea id="note" value={note} onChange={e=>setNote(e.target.value)}/></div><button className="primary" disabled={!note.trim()} onClick={async()=>{if(await action("notes",{body:note}))setNote("")}}>Save note</button></section></aside></div></main></div>}

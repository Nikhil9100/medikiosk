"use client";import { useState } from "react";import { useRouter } from "next/navigation";
export default function HospitalLogin(){const r=useRouter();const [email,setEmail]=useState("");const [password,setPassword]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);async function login(){setBusy(true);setError("");const res=await fetch("/api/staff/login",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({email,password})});const d=await res.json().catch(()=>({}));if(!res.ok){setError(res.status===429?"Too many attempts. Try again later.":"Unable to sign in.");setBusy(false);return;}if(d.staff?.role!=="HOSPITAL"){await fetch("/api/staff/logout",{method:"POST"});setError("This account is not authorised for Hospital Operations.");setBusy(false);return;}r.replace("/hospital");}return <div className="console-shell"><div className="login-card"><p className="eyebrow">Hospital operations</p><h1>Hospital Console</h1><p className="helper">Authorised operational staff only. Access is audited.</p><div className="form-grid"><div className="field"><label htmlFor="email">Email</label><input id="email" type="email" autoComplete="username" value={email} onChange={e=>setEmail(e.target.value)}/></div><div className="field"><label htmlFor="password">Password</label><input id="password" type="password" autoComplete="current-password" value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<div className="system-banner error" role="alert">{error}</div>}<button className="primary" disabled={busy||!email||!password} onClick={()=>void login()}>{busy?"Signing in…":"Sign in"}</button></div></div></div>}

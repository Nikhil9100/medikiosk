import { NextResponse } from "next/server";import { z } from "zod";import { requireStaff } from "@/lib/staff-guard";import { withStaffTx } from "@/lib/db/pool";import { writeAudit } from "@/lib/db/audit";const S=z.object({body:z.string().trim().min(1).max(10000)}).strict();
export async function POST(req:Request,{params}:{params:Promise<{sessionId:string}>}){const g=await requireStaff("DOCTOR");if(g.response)return g.response;const {sessionId}=await params;const p=S.safeParse(await req.json().catch(()=>null));if(!p.success)return NextResponse.json({error:"Invalid note"},{status:400});await withStaffTx(g.staff!.id,async c=>{await c.query(`INSERT INTO doctor_notes(session_id,author_id,body) VALUES($1,$2,$3)`,[sessionId,g.staff!.id,p.data.body]);await writeAudit(c,{type:"STAFF",id:g.staff!.id},"doctor_note.created",{type:"case",id:sessionId});});return NextResponse.json({ok:true},{status:201});}

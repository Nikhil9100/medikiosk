"use client";
import { useEffect,useRef,useState } from "react";import { useRouter } from "next/navigation";import { QUESTIONS,nextQuestion,stateForYesNo } from "@/lib/interview-engine";import { interviewLabel } from "@/lib/interview-localization";import type { InterviewFact,InterviewState } from "@/lib/patient-flow";import { usePatient } from "../PatientShell";
export default function Interview(){const r=useRouter();const {workflow,setWorkflow,sync,t,saveDraft,loadDraft,clearDraft}=usePatient();const [facts,setFacts]=useState(workflow.interviewFacts);const [value,setValue]=useState("");const [busy,setBusy]=useState(false);const [error,setError]=useState("");const [recording,setRecording]=useState(false);const [speaking,setSpeaking]=useState(false);const rec=useRef<MediaRecorder|null>(null);const stream=useRef<MediaStream|null>(null);const abort=useRef<AbortController|null>(null);const timeout=useRef<ReturnType<typeof setTimeout>|null>(null);const audio=useRef<HTMLAudioElement|null>(null);const discard=useRef(false);const q=nextQuestion(facts);useEffect(()=>{void loadDraft<string>("interview-answer-draft").then(d=>{if(d)setValue(d)});return()=>{discard.current=true;abort.current?.abort();if(timeout.current)clearTimeout(timeout.current);if(rec.current&&rec.current.state!=="inactive")rec.current.stop();stream.current?.getTracks().forEach(t=>t.stop());audio.current?.pause()}},[]);// eslint-disable-line react-hooks/exhaustive-deps
useEffect(()=>{if(value)void saveDraft("interview-answer-draft",value);},[value,saveDraft]);
async function save(state:InterviewState,raw?:string,prov:"PATIENT"|"VOICE"|"TOUCH"="PATIENT"){if(!q||busy)return;setBusy(true);setError("");const fact:InterviewFact={questionId:q.id,value:raw?.trim()||undefined,state,provenance:prov};const ok=await sync({interviewData:{[q.id]:fact},workflowStep:"interview"});if(ok){const next={...facts,[q.id]:fact};setFacts(next);setWorkflow(w=>({...w,interviewFacts:next}));setValue("");await clearDraft("interview-answer-draft");}else setError(t("networkError"));setBusy(false);}
async function finish(){if(await sync({workflowStep:"documents"}))r.push("/patient/documents");}
async function voice(){if(!q)return;if(rec.current&&rec.current.state!=="inactive"){rec.current.stop();return;}try{discard.current=false;const s=await navigator.mediaDevices.getUserMedia({audio:true});stream.current=s;const m=new MediaRecorder(s);rec.current=m;const chunks:Blob[]=[];m.ondataavailable=e=>{if(e.data.size)chunks.push(e.data)};m.onstop=async()=>{if(timeout.current)clearTimeout(timeout.current);s.getTracks().forEach(x=>x.stop());rec.current=null;stream.current=null;setRecording(false);if(discard.current)return;try{abort.current=new AbortController();const fd=new FormData();fd.append("audio",new Blob(chunks,{type:m.mimeType||"audio/webm"}),"answer.webm");fd.append("language",workflow.language);const res=await fetch("/api/voice/transcribe",{method:"POST",body:fd,signal:abort.current.signal});if(!res.ok)throw new Error();const d=await res.json() as {transcript:string};if(q.type==="yes_no")await save(stateForYesNo(d.transcript),d.transcript,"VOICE");else setValue(d.transcript);}catch(e){if((e as Error).name!=="AbortError")setError(t("voiceError"));}};m.start();setRecording(true);timeout.current=setTimeout(()=>{if(m.state!=="inactive")m.stop()},30000);}catch{setError(t("voiceError"));}}
async function speakQuestion(){if(!q||speaking)return;setSpeaking(true);try{const res=await fetch("/api/voice/speak",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({text:interviewLabel(q.id,workflow.language,q.label),language:workflow.language})});if(!res.ok)throw new Error();const d=await res.json() as {audioBase64:string;contentType:string};const bytes=Uint8Array.from(atob(d.audioBase64),x=>x.charCodeAt(0));const url=URL.createObjectURL(new Blob([bytes],{type:d.contentType}));const a=new Audio(url);audio.current=a;a.onended=()=>{URL.revokeObjectURL(url);setSpeaking(false)};a.onerror=()=>{URL.revokeObjectURL(url);setSpeaking(false);setError(t("voiceError"))};await a.play();}catch{setSpeaking(false);setError(t("voiceError"));}}
if(!q)return <section className="flow-card reference-card interview-reference-card"><div className="medi-interview-heading"><span className="medi-large-avatar">👩‍⚕️</span><div><p className="eyebrow">7 · Medi guided interview</p><h1>History questions complete</h1></div></div><p className="lead">You can continue to previous medical documents. The doctor will review everything you shared.</p><div className="actions"><button className="primary" onClick={()=>void finish()}>{t("continue")} →</button></div></section>;
const n=QUESTIONS.findIndex(x=>x.id===q.id)+1;const label=interviewLabel(q.id,workflow.language,q.label);return <section className="flow-card reference-card interview-reference-card"><div className="medi-interview-heading"><span className="medi-large-avatar">👩‍⚕️</span><div><p className="eyebrow">7 · Medi guided interview · {n}/{QUESTIONS.length}</p><h1>{t("interviewTitle")}</h1></div></div><div className="panel interview-question-panel"><small>{q.domain.replaceAll("_"," ")}</small><h2>{label}</h2><button className="secondary" disabled={speaking} onClick={()=>void speakQuestion()}>{speaking?"🔊 Playing…":`🔊 ${t("speak")}`}</button>{q.redFlag&&<p className="helper">If this is severe or happening now, alert hospital staff immediately.</p>}</div>{q.type==="yes_no"?<><div className="choice-grid"><button className="choice" onClick={()=>void save("KNOWN","yes","TOUCH")}>Yes</button><button className="choice" onClick={()=>void save("DENIED","no","TOUCH")}>No</button><button className="choice" onClick={()=>void save("UNKNOWN",undefined,"TOUCH")}>{t("unknown")}</button><button className="choice" onClick={()=>void save("DECLINED",undefined,"TOUCH")}>{t("decline")}</button></div><div className="actions"><button className={`mic-button ${recording?"live":""}`} onClick={()=>void voice()}>{recording?`■ ${t("stop")}`:`🎙️ ${t("listen")}`}</button></div></>:<><div className="field"><label htmlFor="answer">Your answer</label><textarea id="answer" value={value} onChange={e=>setValue(e.target.value)}/></div><div className="actions"><button className={`mic-button ${recording?"live":""}`} onClick={()=>void voice()}>{recording?`■ ${t("stop")}`:`🎙️ ${t("listen")}`}</button><button className="primary" disabled={!value.trim()||busy} onClick={()=>void save("KNOWN",value,"PATIENT")}>Save answer →</button></div><div className="actions"><button className="secondary" onClick={()=>void save("UNKNOWN")}>{t("unknown")}</button><button className="secondary" onClick={()=>void save("DECLINED")}>{t("decline")}</button></div></>}{error&&<div className="system-banner error" role="alert">{error}</div>}</section>}

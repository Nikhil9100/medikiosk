"use client";import { useState } from "react";import { useRouter } from "next/navigation";import { validAbhaAddress,validAbhaNumber } from "@/lib/patient-identity";import { usePatient } from "../PatientShell";
export default function Identity(){const r=useRouter();const {mutate,sync,ensureSynced,connection,t}=usePatient();const [number,setNumber]=useState("");const [address,setAddress]=useState("");const [error,setError]=useState("");const [busy,setBusy]=useState(false);
async function submit(skip=false){if(busy)return;setBusy(true);setError("");if(!skip&&number&&!validAbhaNumber(number)){setError("Enter a valid 14-digit ABHA number or leave it blank.");setBusy(false);return;}if(!skip&&address&&!validAbhaAddress(address)){setError("Enter a valid ABHA address ending in @abdm or leave it blank.");setBusy(false);return;}if(!skip&&connection==="offline"){setError("For privacy, full ABHA values are never cached offline. Reconnect to save them, or continue without ABHA.");setBusy(false);return;}let ok=false;if(!skip&&!await ensureSynced()){setError("Reconnect before saving ABHA. Full ABHA values are never cached offline.");setBusy(false);return;}if(skip){const res=await mutate("/api/patient/identity","POST",{skip:true});ok=res.ok;}else{try{const res=await fetch("/api/patient/identity",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({abhaNumber:number||undefined,abhaAddress:address||undefined})});ok=res.ok;}catch{ok=false;}}if(!ok){setError(skip?"Could not save the skip choice. Please retry.":"Identity details could not be saved safely. Your full ABHA value was not cached.");setBusy(false);return;}if(await sync({workflowStep:"complaint"})){setNumber("");setAddress("");r.push("/patient/complaint");return;}setBusy(false);}
return <section className="flow-card reference-card identity-reference-card"><div className="centered-reference-heading"><div className="reference-icon-badge id-card" aria-hidden="true">▣</div><p className="eyebrow">3 · ABHA (Optional)</p><h1>Link Your ABHA <span className="soft-title">(Optional)</span></h1><p className="lead">Add an Ayushman Bharat Health Account identifier only if you want to. You can continue without it.</p></div>
<div className="abha-info-card"><strong>What happens to this identifier?</strong><span>Only a privacy-minimised hint/last four digits is retained for a self-declared ABHA. This does not verify your identity or claim live ABDM connectivity.</span></div>
<div className="form-grid reference-form"><div className="field"><label htmlFor="abha-number">ABHA number (optional)</label><input id="abha-number" inputMode="numeric" maxLength={17} placeholder="14-digit ABHA number" value={number} onChange={e=>setNumber(e.target.value)}/></div><div className="field"><label htmlFor="abha-address">ABHA address (optional)</label><input id="abha-address" autoCapitalize="none" placeholder="name@abdm" value={address} onChange={e=>setAddress(e.target.value)}/></div></div>
<div className="privacy-inline">🔒 Full ABHA values are never cached in the offline recovery draft.</div>{error&&<div className="system-banner error" role="alert">{error}</div>}
<div className="reference-actions stacked-mobile"><button className="primary reference-primary" disabled={busy} onClick={()=>void submit(false)}>{busy?t("processing"):"Continue with ABHA"}</button><button className="secondary" disabled={busy} onClick={()=>void submit(true)}>Continue without ABHA</button></div></section>}
